apiVersion: v1
kind: Service
metadata:
  name: {{ include "traveloop.fullname" . }}
  labels:
    app: {{ include "traveloop.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: {{ include "traveloop.name" . }}
